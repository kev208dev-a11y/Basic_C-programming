{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C 파일 실행",
            "type": "cppdbg",
            "MIMode": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C 컴파일",
            "environment": [
                {
                    "name": "LANG",
                    "value": "ko_KR.UTF-8"
                }
            ]
        }
    ]
}